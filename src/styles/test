#header {
 display: flex;
 justify-content: space-between;
 background-color: var(--bcolor);
 border-bottom: solid 2px var(--bcolor);
 height: 5em;
 left: 0;
 line-height: 5em;
 position: fixed;
 top: 0;
 width: 100%;
 z-index: 100; /* Replace _misc(z-index-base) */


}


.index-link {
 font-size: clamp(1em, 2vw, 3em); /* Responsive text size */
 padding: 0.5em 1em;
 white-space: nowrap;
 display: flex;
 align-items: center;
 }






#header a {
 color: inherit;
 text-decoration: none;
 font-size: 10em;
 flex: 1 1 20%; /* Fixed minimum width */
}


#header ul {
 list-style: none;
 margin: 0;
 padding-left: 0;


}


#header ul li {
 display: inline-block;
 padding: 0;
}


#header h1 {
 margin: 0;
 line-height: 1;
 padding: 0 1em;
 white-space: nowrap;
 display: flex;


}


#header h1 a {
 font-size: 1.2em;
 color: var(--pcolor);
}


#header .links {
 flex: 1;
 border-left: solid 3px var(--pcolor); /* Replace _palette(border) */
 line-height: 3em;
 overflow: hidden;
 padding-left: 1.5em;
 font-size: 1.7em;
 font-size: clamp(1.2em, 2vw, 1.7em); /* Responsive text size */
 max-height: 5em;
}


#header .links ul li {
 border-left: solid 3px var(--pcolor); /* Replace _palette(border) */
 line-height: 1;
 margin-left: 1em;
 padding-left: 1em;
}


#header .links ul li:first-child {
 border-left: 0;
 margin-left: 0;
 padding-left: 0;
}


#header .links ul li a {
 border-bottom: 0;
 font-family: inherit; /* Replace _font(family-heading) */
 font-size: 0.7em;
 font-weight: bold; /* Replace _font(weight-heading) */
 letter-spacing: 0.1em; /* Replace _font(kerning-heading) */
 text-transform: uppercase;
 color: var(--pcolor); /* Replace _palette(fg-light) */
}


#header .main {
 height: inherit;
 line-height: inherit;
 text-align: right;
}


#header .main ul {
 height: inherit;
 line-height: inherit;
}


#header .main ul li {
 border-left: solid 2px #ddd; /* Replace _palette(border) */
 height: inherit;
 line-height: inherit;
 white-space: nowrap;
}


#header .main ul li > a {
 text-decoration: none;
 border-bottom: 0;
 color: var(--pcolor); /* Replace _palette(fg-light) */
 overflow: hidden;
 position: relative;
 text-indent: 4em;
 width: 4em;
}


#main {
 flex: 1; /* Let the main content fill the remaining space */
 padding: 2em; /* Optional padding for spacing */
 background-color: var(--pcolor);
 color: var(--bcolor);
 max-width: 100%;  
}




/* find not found*/


#lost{
 display: flex;
 flex-direction: column;
 justify-content: center;
 align-items: center;
}


.blurb {
 margin-bottom: 8px; /* Adjust this value to control spacing */
 line-height: 2;
}


article{
 padding: 1em;
 padding: 1em;
 outline: 2px solid var(--bcolor);


}


article h1 {
 margin: 1em;
 line-height: normal;
}





